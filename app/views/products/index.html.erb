<div class="container-fluid px-3">
  <h1 class="text-center my-3">Products</h1>
  <%= form_with url: filter_products_path, class:"mb-5" do |form| %>
    <div class="row my-2 gx-0">
      <div class="col-10 p-0">
        <%= form.select( :category_id, options_for_select(Category.all.collect {|c| [c.title, c.id]}.unshift(['Select Category', nil])), {}, { class: "form-select"})%>
      </div>
      <div class="col-2 p-0">
        <%= form.submit "Search", class: "btn btn-primary w-100" %>
      </div>
    </div>
  <% end %>
  <%if current_user && current_user.admin?%>
    <div class="row my-2 gx-0">
      <div class="col-12 p-0">
        <%= link_to 'Add New Product', new_product_path,  class: "btn btn-success w-100" %>
      </div>
    </div>
    <table class="table table-striped">
      <thead class="bg-dark text-white ">
        <tr>
          <th >Image</th>
          <th>Title</th>
          <th>Description</th>
          <th>Price</th>
          <th>Category</th>
          <th ></th>
        </tr>
      </thead>
      <tbody>
        <% @products.each do |product| %>
          <tr>
            <% if product.image.attached? %>
              <td><%= cl_image_tag(product.image.url, width: 200, height: 150, :crop=>"scale") %></td>
            <% end%>
            <td style="vertical-align:middle; <%= 'color: red' unless product.available%>"><%= product.title %></td>
            <td style="vertical-align:middle"><%= product.description %></td>
            <td style="vertical-align:middle"><%= product.price %></td>
            <td style="vertical-align:middle">
              <% product.categories.each do |cat| %>
                <%= cat.title%>
                <%= ',' unless product.categories.last==cat%>
              <%end%>
            </td>
            <td style="vertical-align:middle">
              <%= link_to 'Show', product, class: "me-3"%>
              <%= link_to 'Edit', edit_product_path(product), class: "me-3"%>
              <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }, class: "me-3"%>
              <%if product.available%>
                <%= link_to 'Suspend', retire_product_path(:id => product.id), data: { confirm: 'Are you sure?' }, class: "me-3"%>
              <%else%>
                <%= link_to 'Resume', resume_product_path(:id => product.id), data: { confirm: 'Are you sure?' }, class: ""%>
              <%end%>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
  <%else%>
    <div class="d-flex flex-wrap justify-content-between">
      <% @products.each do |product| %>
        <% if product.available%>
          <%= link_to product, class: "text-decoration-none" do%>
            <div class="card mb-4 me-4" style="width: 12rem;">
              <%if product.image.attached?%>
                <%= image_tag product.image , style: "height: 9rem", class: "card-image-top w-100" %>
              <%end%>
              <div class="card-body">
                <h5 class="card-title mb-0"><%= product.title.capitalize %></h5>
                <div class="price">Rs. <%= product.price %></div>
                <p class="card-text text-muted"><%= product.description%></p>
              </div>
              <%= link_to "Add to cart", line_items_path(:product_id => product.id), method: :post, class: "btn btn-outline-success w-100" %>
            </div>
          <%end%>
        <%end%>
      <%end%>
    </div>
  <%end%>
  <%= link_to 'Clear Filter', products_path if @is_filtered %>
</div>
 <%# <%= cl_image_tag(product.image,:width=>200, :height=> 150,:crop=>"scale", style: "height: 9rem", class: "card-image-top w-100")%>
